<% if @feed_items.any? %>
  <ol class="menu_items">
    	<%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= ajax_will_paginate @feed_items, :params => { :my_excluded_param => nil } %>
<% end %>